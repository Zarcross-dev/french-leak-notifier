services:
  french-leak-notifier:
    build: .
    image: french-leak-notifier:latest
    container_name: french-leak-notifier
    restart: unless-stopped
    env_file:
      - .env
    environment:
      - DATA_DIR=/app/data
    volumes:
      - leak-data:/app/data

volumes:
  leak-data:
